# Rehearsal — direction contract

User delegated continued implementation after a concrete concept proposal. Code-first is an explicit standing preference. Product facts and stack assumptions are in PRODUCT.md. No further design approval requested.

Mode: Operate. The audience is a developer assessing release risk, in daylight with a code editor nearby. The interface must expose actual query evidence and the difference between a smoke test and a rollout rehearsal within its first viewport.

Grounded systems considered, in order: (1) CI job timeline, (2) database query console, (3) engineering verification worksheet, (4) release operations board, (5) version-control split diff, (6) transaction ledger, (7) network sequence diagram. Selected by assigned index 3: engineering verification worksheet.

World: warm paper, near-black typography, restrained cobalt for actions, oxblood for actual failures, forest green for actual passes. Ruled rows, compact labels, precise alignment, generous margin around the current migration. IBM Plex Sans for interface, Plex Mono only SQL and measured data. Actual operations are a column of phase rows; selecting a phase reveals assertions and actual vs expected outputs.

FIRST VIEWPORT: Rehearsal identity and local execution status. A compact project/case selector at left. Main heading 'A migration can pass. Your rollout can still fail.' A SQL editor alongside the phase sequence. Clear 'Run rehearsal' action. No invented passing run on first load. Original/reference/custom candidate provenance remains visible. Reports show new-only smoke success and mixed-version failure in the same composition. Footer discloses PGlite and sequential-contract limits.

Signature interaction: the stage sequence fills with real worker results; clicking an assertion opens its SQL, expected rows, actual rows/error. No fake typing, strobing, or fake background agents.

Challenger verdicts: data-sublime field declined on identification and clarity; keep precision of column alignment. Split-flap concourse competitive on scanability but less familiar for query evidence; keep fixed stage positions. Streaming wall declined on both; keep focused selection without moving controls. Airport wayfinding declined on both; keep one obvious next action. Deep dive declined on both; keep chronological phase orientation. Tensegrity declined on both; keep relationship between failed assertion and affected version. These raise information discipline, not decorative motifs.

Responsive: sidebar becomes a case select; editor and phases stack; code scrolls inside its container. Reduced motion removes row transitions. Keyboard-visible focus, native buttons, labeled textarea, live status, no nested cards.

Risk: three sample contracts do not establish general migration safety. Limitations must stay visible; sample reference repair must never be attributed to Bob.

FORM: Assigned direction seed key 5ff574bc, candidate index 3 (engineering verification worksheet), generated in this build's initial design pass. The chosen world is expressed directly in code under the user's explicit code-first instruction. No generated comp or image-approval round applies.

THESIS: Show the gap between a successful migration and compatible application behavior with executable evidence.
OWN-WORLD: Warm paper #f5f4ef; dark ink #232722; cobalt #2446c8 action; red #983a31 failure; green #216046 pass. Ruled query worksheet with paired editor and phase rows.
STORY: Run risky SQL, inspect the failed contract, send that evidence to Bob, rerun a repair against unchanged assertions.
Implementation adaptation: the heading is shortened to 'Test the whole rollout.' with the original distinction in the subheading for narrow viewports. The mobile case selector uses three labeled tabs, avoiding a hidden menu for the three-case scope.
